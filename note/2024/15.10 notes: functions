// low stakes quiz
1) which operator do we use for integer division?
//
2) when storing currency what data type do we need to use?
float
3) what data type would you recommend for storing an email
string
4) what would 10 MOD 3 =
1
5) describe the purpose of casting?
turn one data type inrto another


//task
a peramiter is something that gets put into and used by the function
can 2 functions have the same name?
no
type the function name and any peramiters / arguments it has
we can use print(function)
a local variable is a variable native to a function
